when i try to connnect thr  fronted  to  backned  there is  an  error that  might  we occure  the  erroer  was  cros  error 

cross  origin  resourse  sharing  in  expres 
 cros  is a  securiyt   feature  in  web  bbrewaser  thay  prevent  request  from  diffrent  ofigin  unless  explicitly  allowaed  by  the  server 
 
 same  origin  policy  restricts  request  from  diffreant  origin  { protocal  domai  or port} agr  domain chage hai  to error or port  hai  roh  error 

 cors  allows  server  to  specify  to  specify  who  can naccess  theire  resourse 
it behave like a middleware in backned  
 install  it  use  
 npm  i cors 

function App() {

  async function getRes() {
    const res = await fetch("http://localhost:3000/")
    let data = res.json();
    console.log(data);
  }

  return (
    <div>
      <button  onClick={()=> getRes()}> Send</button>
    </div>
  )
}




insted of  using the  fetch api  we usi the  axios  for the  fethcing the  backned data to  the  fronted

middleware status  code  and  http headers 

middleware  runs  before  the  route  handler  
middleware  must call  next()  to  continue  to  the  next  function 
  if   middleware  does  not  call  next()  the request  will  hang .
    jthere  are  some  build  in  consotm  ,  third  party  middleware etc ,

     app.use(cors()); --  third  party middleware
     app.use(expres.json()) --  build  in  middleware

      app.use((req,  res ,  next ) = >{
        console.log("costome  middleware");     
        next();    --         custom  middleware
      })
status code  

informational  response 100 -190
successfull  response   200 -  299
redirection  messages  -  300 -  399 
client  error  response  400 -499 
server error  response -  500 -  599

app.post("/", (req, res) => {
    
    console.log(req.body);
    res.status(400).send({               //  used for the  custom  status  
        success: true
    })
    
})
      

Http  headers ----

 http  header are  key  value  paire  used  in  http  request  and  response to  pass 
 attitional  information between the client  and  server

 they  help  in defining  metadata , specifiying  content  types  ,  setting   ,
 authentication  token etc.


 types  of  http  header :

 1 request headers  :  sent  by  the  client  to  the  server (user Agent)
 2 response  Header :  sent  by  the server  to  the  client (Content -  types)  useed in  x-name of header  starting with x 


https header 
 get  request  header 
 1.  req.get -  for  getting  the  specific  header 
2 .  req.headers -  for  getting all  headers

 -  fet  response  headers :  use.res.set()  or  res.header()
 remove  header  :  use res.removeHeader(header name )

 in

 
// to  print  ithe  header

app.get("/", (req, res) => {
    console.log(req.headers); //
    res.json({
        name: "Asit kumar",
        age :21
    })
    
})


print the  specific  header  
 "    console.log(req.get("user-agent")); //  name  of the header 
"


 